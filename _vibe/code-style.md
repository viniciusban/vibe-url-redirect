# Code style

Return early as much as possible from functions and methods. Prefer flat code over nested conditions. Prefer readibility over performance, e.g., prefer loop iteration over recursion (when in doubt, ask). Put as less logic as possible in views. Move business logic to services.
